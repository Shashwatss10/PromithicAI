# PromithicAI v2.0 - Backend Dependencies
# Using flexible version ranges to avoid conflicts

# API Framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# LangGraph + LangChain
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-anthropic>=0.1.0
langchain-openai>=0.1.0

# OpenAI SDK (also for NVIDIA NIM - OpenAI-compatible)
openai>=1.30.0

# Firebase Admin (ID token verification)
firebase-admin>=6.0.0

# HTTP + Utilities
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
